## 1. Бекенд: валідація параметрів пагінації

- [x] 1.1 Додати в `ListProjectsRequest::rules()` правила `page` (`nullable|integer|min:1`) і `limit` (`nullable|integer|in:10,20,50`) поруч із наявними `search` і `searchJoin`; перевірка: `/projects?limit=1000` і `/projects?page=0` повертають помилку валідації, а `/projects?limit=20&page=2` проходить

## 2. Фронтенд: спільна робота з query-параметрами адреси

- [x] 2.1 Додати в `resources/js/shared/lib/` модуль роботи з query-рядком — читання параметрів з `page.url` в об'єкт і збирання query-об'єкта з відкиданням `null`, `undefined` і порожніх рядків — та експортувати з `shared/lib/index.ts`; перевірка: `npm run type-check` проходить, а збирання об'єкта з `{ search: 'title:сайт', page: null, limit: 20 }` дає рівно два ключі
- [x] 2.2 Перенести `limit` з поточної адреси в `useProjectFilters.apply()` через цей хелпер, не додаючи `page`; перевірка: у браузері при обраному розмірі сторінки 50 зміна фільтра лишає `limit=50` в адресі, а `page` зникає (сценарії «Розмір сторінки переживає зміну фільтра» і «Розмір сторінки переживає скидання фільтрів»)

## 3. Фронтенд: переввикористовуваний компонент пагінації

- [x] 3.1 Додати `resources/js/shared/ui/Pagination.vue` — обгортку PrimeVue `Paginator`, що приймає `Pagination` з `shared/types` і перелік дозволених розмірів сторінки, перетворює 0-базовий номер із події `@page` на 1-базовий і емітить два окремі наміри: зміну сторінки і зміну розміру сторінки; експортувати з `shared/ui/index.ts`; перевірка: `npm run build` проходить і `Paginator` з'являється в перегенерованому `components.d.ts`
- [x] 3.2 Обмежити в цьому компоненті номер сторінки реальною кількістю сторінок при обчисленні зміщення для `Paginator`; перевірка: при `current_page: 99` і `total_pages: 2` панель показує дві сторінки з активною другою, а не порожній чи зламаний стан (сценарій «Номер сторінки більший за кількість сторінок»)
- [x] 3.3 Переконатися, що зміна значення в селекторі розміру сторінки породжує рівно один намір (а не намір «нова сторінка» додатково до «новий розмір»); перевірка: у браузері після 4.4 зміна розміру сторінки дає один запит у Network, а не два

## 4. Фронтенд: підключення пагінації до списку проєктів

- [x] 4.1 Створити слайс `resources/js/features/project-pagination/` з `index.ts` і моделлю, що будує адресу з поточних query-параметрів: перехід на сторінку зберігає всі інші параметри, зміна розміру сторінки прибирає `page`, а дефолтні значення (перша сторінка, розмір 10) в адресу не потрапляють; перевірка: `npm run type-check` проходить, а перехід на другу сторінку з активним фільтром лишає `search` і `searchJoin` в адресі
- [x] 4.2 Реалізувати в моделі Inertia-навігацію на `projects.index` з `preserveState`, `preserveScroll`, `only: ['projects']` і **без** `replace`; перевірка: після переходу на другу сторінку кнопка «назад» у браузері повертає першу сторінку (сценарій «Кнопка назад»)
- [x] 4.3 Додати в слайсі UI-обгортку над `shared/ui/Pagination.vue`, яка приймає `meta.pagination` зі сторінки і викликає модель; перевірка: компонент рендериться на `/projects` і перемикає сторінки
- [x] 4.4 Відрендерити панель під сіткою проєктів у `pages/projects/ui/ProjectsPage.vue` з умовою показу — результатів більше, ніж вміщує сторінка, або `limit` присутній в адресі, або сторінка не перша; перевірка: при 4 проєктах панелі немає, при обраному розмірі 50 і тих самих 4 проєктах панель видима, при 15 проєктах панель показує 2 сторінки
- [x] 4.5 Перевірити, що панель лишається видимою при порожньому результаті через `page` поза діапазоном, а на першій сторінці порожній список і далі показує наявні empty-state без панелі; перевірка: `/projects?page=99` показує панель і дозволяє повернутися, `/projects` без проєктів показує повідомлення з кнопкою створення, а `/projects` з фільтром без збігів — повідомлення без кнопки

## 5. Фінальна перевірка

- [x] 5.1 Перевірити збереження стану через адресу вручну: другу сторінку з розміром 20 перезавантажити, пройти «назад»/«вперед» і відкрити скопійоване посилання; перевірка: у всіх випадках показаний зріз списку і стан панелі відповідають параметрам адреси, а на першій сторінці з розміром 10 адреса чиста від `page` і `limit`
- [ ] 5.2 Прогнати перевірки якості — `composer fixer`, phpstan за конфігом `phpstan.neon.dist` і `npm run type-check`; перевірка: усі команди без помилок
- [x] 5.3 Пройти вручну сценарії зі `specs/project-list-pagination/spec.md` і змінені вимоги зі `specs/project-list-filtering/spec.md`, зокрема контракт параметрів на бекенді — дефолт 10 записів, `limit=20`, відмова на `limit=1000`, `limit=0` і `page=0`, порожній результат на `page=99`; перевірка: жоден сценарій не залишився неперевіреним
