## 1. Каркас тестів контейнера Project

- [x] 1.1 Створити відсутню тестову інфраструктуру контейнера — `app/Containers/AppSection/Project/Tests/ContainerTestCase.php`, `Tests/FunctionalTestCase.php`, `Tests/Functional/WebTestCase.php` — за зразком однойменних файлів контейнера `TimeEntry`; перевірка: `docker compose exec app php artisan test --filter=Project` запускається без помилок завантаження класів (тестів ще немає)
- [x] 1.2 Написати функціональний тест `Tests/Functional/WEB/ListProjectsTest.php` з `#[CoversClass(ListProjectsController::class)]`, який фіксує поточну поведінку списку без фільтрів: власний проєкт користувача і проєкт, де він лише учасник, обидва присутні в Inertia-пропсі `projects`, а чужий проєкт відсутній; перевірка: тест проходить до будь-яких змін у коді (створення проєктів — через `Project::create()`, як у `TimeEntry`-тесті, бо фабрики проєкту немає)

## 2. Бекенд: увімкнути фільтрацію через RequestCriteria

- [x] 2.1 Заповнити `ProjectRepository::$fieldSearchable` значеннями `'title' => 'ilike'` і `'status' => 'in'` замість закомментованого прикладу; перевірка: запит `/projects?search=title:сайт&searchJoin=and` звужує список, тобто `addRequestCriteria()` перестав бути інертним
- [x] 2.2 Обгорнути OR-групу в `ProjectRepository::scopeAuthUserId()` у вкладене замикання (за зразком `TaskRepository::scopeVisibleForUser`); перевірка: тест з 1.2 (список без фільтрів) продовжує проходити
- [x] 2.3 Додати мінімальні правила у `ListProjectsRequest::rules()` — `search` як `nullable|string|max:512`, `searchJoin` як `nullable|string|in:and,or` — без парсингу мінісинтаксису бібліотеки; перевірка: тест на `search` довжиною 513 символів отримує відмову валідації, а звичайні запити проходять

## 3. Бекенд: тести поведінки фільтрів

- [x] 3.1 Дописати в `ListProjectsTest` покриття фільтра за назвою — частковий збіг, регістронезалежність (перевіряє саме `ilike`, а не `like`), збіг лише в описі не враховується, збіг за межами першої сторінки (створити більше проєктів, ніж вміщує сторінка); перевірка: `docker compose exec app php artisan test --filter=ListProjectsTest` зелений
- [x] 3.2 Дописати покриття фільтра за статусами — `search=status:in_progress`, кілька значень `search=status:draft,on_hold`, невідоме значення `search=status:archived` дає порожній список без помилки, невідоме поряд з відомим повертає відоме; перевірка: той самий запуск тестів зелений
- [x] 3.3 Дописати покриття комбінації полів — `search=title:сайт;status:in_progress&searchJoin=and` звужує за обома умовами, і той самий запит **без** `searchJoin=and` дає ширший результат (фіксує, чому параметр обов'язковий); перевірка: обидва твердження виконуються
- [x] 3.4 Дописати покриття межі видимості під фільтром — чужий проєкт зі збігом відсутній; проєкт, де користувач лише учасник, підпадає під фільтр за статусом і відсіюється, коли статус не збігається; перевірка: тимчасове прибирання обгортки з 2.2 робить саме тест про відсіювання проєкту-членства червоним

## 4. Фронтенд: спільні будівельні блоки

- [x] 4.1 Додати debounce-утиліту в `resources/js/shared/lib/` і експортувати її з `shared/lib/index.ts` поруч з наявними; перевірка: `npm run type-check` проходить, утиліта імпортується з `@/shared/lib`
- [x] 4.2 Додати `resources/js/shared/ui/form-fields/MultiSelect.vue` з контрактом пропсів за зразком `Select.vue` і експортувати як `FormMultiSelect` з `form-fields/index.ts`; перевірка: `npm run build` проходить і `MultiSelect` з'являється у перегенерованому `components.d.ts`
- [x] 4.3 Додати в `entities/project/model/types.ts` тип значень фільтрів (`search` + список `ProjectStatus`) і хелпер «чи активні фільтри», експортувавши їх з `entities/project/index.ts`; перевірка: `npm run type-check` проходить, `STATUS_OPTIONS` не дублюється новим переліком міток

## 5. Фронтенд: робота з синтаксисом search

- [x] 5.1 Створити слайс `resources/js/features/project-filters/` з `index.ts` і модулем, що формує рядок `search` за синтаксисом бібліотеки — `title:<текст>` і `status:<s1>,<s2>` через `;`, разом із `searchJoin=and`, з префіксом поля навіть коли вибрано лише текст, і без порожніх частин; перевірка: для стану «текст + два статуси» отримуємо `title:сайт;status:draft,on_hold`, для стану «лише статуси» — `status:draft,on_hold`, для порожнього стану — жодного параметра
- [x] 5.2 Додати в цей самий модуль очищення тексту від `:`, `;`, `%`, `_` і обрізання пробілів перед формуванням `search`, а також розбір рядка `search` з `usePage().url` у структурований стан панелі; перевірка: введення «Етап: 1» дає `title:Етап 1`, введення «%» не додає фільтр за назвою, а розбір щойно сформованого рядка повертає початковий стан
- [x] 5.3 Реалізувати навігацію при зміні фільтрів — Inertia-запит на `projects.index` з `preserveState`, `preserveScroll`, `replace`, `only: ['projects']` і без параметра `page`; перевірка: у браузері зміна фільтра оновлює URL і список, а `page` зникає з адреси

## 6. Фронтенд: панель фільтрів

- [x] 6.1 Додати UI панелі у слайсі — `FormText` для назви з debounce 300 мс і `FormMultiSelect` для статусів на `STATUS_OPTIONS` із застосуванням одразу; перевірка: набирання тексту робить один запит після паузи, а не на кожен символ (видно у Network)
- [x] 6.2 Додати кнопку скидання, активну лише за наявності активних фільтрів, яка очищає обидва поля і повертає адресу без `search` і `searchJoin`; перевірка: після скидання поле пусте, статуси не вибрані, URL без параметрів фільтрів, список повний

## 7. Фронтенд: інтеграція на сторінці

- [x] 7.1 Відрендерити панель фільтрів над сіткою проєктів у `pages/projects/ui/ProjectsPage.vue`; перевірка: `npm run type-check` проходить, панель видна на `/projects`, а пропси сторінки не змінилися (контролер не віддає нових даних)
- [x] 7.2 Розрізнити два empty-state через наявний `EmptyList` — за активних фільтрів опис про відсутність знайдених проєктів і `show-action="false"`, інакше поточний варіант із кнопкою створення; перевірка: пошук за неіснуючою назвою показує повідомлення без кнопки, а порожній список без фільтрів — з кнопкою
- [ ] 7.3 Перевірити збереження стану через URL: застосувати обидва фільтри, перезавантажити сторінку, скористатися кнопкою «назад» і відкрити скопійоване посилання; перевірка: у всіх трьох випадках поля панелі і список відповідають параметрам адреси

## 8. Фінальна перевірка

- [ ] 8.1 Прогнати перевірки якості — `docker compose exec app php artisan test`, `composer fixer`, phpstan за конфігом `phpstan.neon.dist` і `npm run type-check`; перевірка: усі команди без помилок, зокрема наявний `app/Ship/Tests/Unit/Configs/RepositoryTest.php` (конфіг репозиторію не змінювався)
- [ ] 8.2 Пройти сценарії зі `specs/project-list-filtering/spec.md` вручну на `/projects` і переконатися, що кожен має відповідне покриття тестом або підтверджений вручну результат; перевірка: жоден сценарій специфікації не залишився неперевіреним
