Частина роботи вже лежить у робочому дереві незакоміченою. Такі пункти сформульовані як звірка з вимогами — якщо код уже відповідає, пункт закривається перевіркою, а не переписуванням.

## 1. Бекенд: фільтрація списку задач

- [ ] 1.1 Заповнити `TaskRepository::$fieldSearchable` (`title` → `ilike`, `status` → `in`) і переконатися, що закоментований приклад прибрано
- [ ] 1.2 Додати `addRequestCriteria()` у ланцюжок `ListTasksTask::run()` перед `scopeVisibleForUser()` і перевірити вручну, що `/tasks?search=title:звіт&searchJoin=and` звужує список
- [ ] 1.3 Звірити `TaskRepository::scopeVisibleForUser()` з вимогою «Комбінування фільтрів і збереження межі видимості»: увесь OR має лишатися в одній групі `where(function ($q) {...})`, інакше фільтр перестане діяти на одну з гілок видимості — змін не потрібно, потрібне підтвердження перед тестами групи 5



## 2. Бекенд: валідація запиту

- [ ] 2.1 Додати в `ListTasksRequest::rules()` правила `search` (`nullable`, `string`, `max:512`) і `searchJoin` (`nullable`, `string`, `in:and,or`); верифікація — тести 5.6 і 5.7



## 3. Фронтенд: спільний синтаксис `search`

- [x] 3.1 Звірити `shared/lib/search-param.ts` з рішенням 6 у `design.md`: модуль знає лише про формат (розділювачі, розбір і збирання `search`, списки значень, очищення зарезервованих символів, `SEARCH_JOIN`) і не містить нічого про задачі чи статуси
- [ ] 3.2 Перевірити, що `parseSearchParam` ігнорує частини без `:` і не ламається на порожньому чи відсутньому `search`, а `sanitizeSearchText` прибирає `:`, `;`, `%`, `_` і обрізає пробіли — сценарії вимоги «Зарезервовані символи в тексті пошуку»
- [ ] 3.3 Звірити публічні експорти `shared/lib/index.ts` з тим, що реально потрібно споживачам, і прибрати з них те, що лишилося без використання



## 4. Фронтенд: доменний шар фільтрів задач

- [ ] 4.1 Додати `isTaskStatus` у `entities/task` поряд із `TASK_STATUS` і експортувати з `index.ts`; верифікація — пункт 4.2
- [ ] 4.2 Застосувати `isTaskStatus` у `parseFilterQuery` замість приведення `as TaskStatus[]` і перевірити, що `/tasks?search=status:archived,todo&searchJoin=and` лишає у виборі статусів лише `todo` — вимога «Стійкість панелі до довільних значень в адресі»
- [ ] 4.3 Ініціалізувати стан фільтрів розбором `usePage().url` у момент створення замість `onMounted` і перевірити, що при відкритті посилання з фільтрами панель не блимає порожньою
- [ ] 4.4 Додати `watch` на `page.url`, який перезаписує стан лише за розбіжності побудованого query (рішення 9 у `design.md`), і супроводити його коротким коментарем про те, чому порівнюється query, а не сирі значення
- [ ] 4.5 Перевірити «назад» і «вперед» у браузері після застосування фільтрів: панель і список щоразу описують один стан — вимога «Панель відповідає адресі від першого рендера і при навігації історією»
- [ ] 4.6 Перевести навігацію в `useTaskFilters` з глобального `route('tasks.index')` на `taskRoutes.index()` з `entities/task` і переконатися, що запит іде на той самий маршрут
- [ ] 4.7 Звірити параметри Inertia-візиту з рішенням 11: `preserveState`, `preserveScroll`, `replace`, `only: ['tasks']`, дебаунс 300 мс для тексту і миттєве застосування для статусів



## 5. Тести контейнера `Task`

- [ ] 5.1 Створити базові класи тестів контейнера (`Tests/ContainerTestCase.php`, `Tests/FunctionalTestCase.php`, `Tests/Functional/WebTestCase.php`) за зразком контейнера `Project` і переконатися, що порожній тест-кейс запускається
- [ ] 5.2 Написати `Tests/Functional/WEB/ListTasksTest.php` з хелперами створення задач (проєктна, безпроєктна, чужа) і збирання параметрів `search`/`searchJoin`; верифікація — базовий тест «список без фільтрів показує обидві гілки видимості»
- [ ] 5.3 Покрити фільтр за назвою: частковий збіг, ігнорування регістру, збіг за межами показаного зрізу, ігнорування опису — вимога «Фільтрація за назвою задачі»
- [ ] 5.4 Покрити фільтр за статусами: один статус, кілька статусів, невідоме значення дає порожній список, невідоме поряд з відомим лишає відоме — вимога «Фільтрація за статусами задачі»
- [ ] 5.5 Покрити межу видимості під фільтрами в обох гілках: задача проєкту-членства підпадає і відсіюється, безпроєктна задача виконавця підпадає, безпроєктна задача автора відсіюється за статусом, чужі задачі обох видів не з'являються — вимога «Комбінування фільтрів і збереження межі видимості»
- [ ] 5.6 Покрити контракт параметрів: комбінація `title` і `status` за AND, запит без `searchJoin` дає ширший результат, `search` довжиною 513 символів відхиляється
- [ ] 5.7 Покрити відхилення `searchJoin`, відмінного від `and`/`or`
- [ ] 5.8 Запустити тестовий набір контейнера і переконатися, що всі тести групи 5 проходять на Postgres-конфігурації



## 6. Сторінка задач

- [ ] 6.1 Звірити рендер панелі фільтрів і двох empty-state у `TasksPage.vue` з вимогою «Розрізнення порожнього результату та порожнього списку»: за активних фільтрів повідомлення не пропонує створити першу задачу
- [ ] 6.2 Перевірити, що керування скиданням неактивне без фільтрів і що після скидання адреса не містить `search` і `searchJoin` — вимога «Скидання фільтрів»
- [ ] 6.3 Перевірити вручну сценарії панелі, які не покриті тестами: перезавантаження сторінки з фільтрами, відкриття скопійованого посилання, набирання тексту без окремого запису в історії на кожен символ, збереження фокуса в полі під час застосування



## 7. Завершення

- [ ] 7.1 Пройти `specs/task-list-filtering/spec.md` за списком вимог і відмітити, чим саме закритий кожен сценарій — тестом із групи 5 чи ручною перевіркою з груп 4 і 6
- [ ] 7.2 Перевірити лінтери фронтенду і статичний аналіз бекенду на змінених файлах