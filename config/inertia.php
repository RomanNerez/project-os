<?php

return [

    'testing' => [

        /*
        | Pages live in `resources/js/pages/{name}/index.ts`, which Inertia's
        | view finder cannot resolve — it looks for `{name}.{ext}` files only.
        */
        'ensure_pages_exist' => false,

    ],

];
